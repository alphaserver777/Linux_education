команда ls показывает содержимое директории
команда ls -l показывает расширенную информацию о содержимом директории
команда ls -a показывает скрытие файлы в директории
скрытые файлы начинаюся с точки т.к так исторически сложилось
