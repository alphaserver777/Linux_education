# Домашнее задание

## Глава 4 — Операции с файлами и каталогами

---

# Цель домашней работы

Закрепить навыки:

- создания файлов и каталогов;
- копирования и перемещения объектов;
- удаления файлов;
- работы с абсолютными и относительными путями;
- создания ссылок;
- исследования структуры файловой системы Linux.

---

# Правила выполнения

1. Все задания выполнять только через терминал.
2. Не использовать графический файловый менеджер.
3. После каждого задания проверять результат командами:

    - `ls`
    - `pwd`
    - `tree`

4. Все команды вводить вручную.
5. Сохранять историю команд.

---

# Часть 1 — Навигация

---

# Задание 1

## Требуется:

1. Открыть терминал
2. Определить текущий рабочий каталог
3. Перейти:

    - в `/usr`
    - затем в `/usr/bin`

4. Вернуться:

    - сначала в `/usr`
    - затем в домашний каталог

---

## Вопросы

1. Что показывает команда `pwd`?
2. Чем абсолютный путь отличается от относительного?
3. Что означает:

    - `.`
    - `..`
    - `~`

## Ответы
1. Текущий каталог
2. Абсолютный используется относительно текущего каталога
3. "." - Текущий каталог, ".." - На каталог выше, "~" - Домашний каталог
---

---

# Задание 2

## Требуется:

Перейти в следующие каталоги и изучить их содержимое:

```text
/etc
/var
/tmp
/home
```

Для каждого каталога:

1. Выполнить `pwd`
2. Выполнить `ls`
3. Кратко описать назначение каталога

---

# Часть 2 — Работа с каталогами

---

# Задание 3

## Требуется:

Создать структуру каталогов:

```text
education/
├── linux/
│   ├── theory/
│   ├── practice/
│   └── homework/
├── scripts/
└── backup/
```

---

## Дополнительно

После создания:

1. Перейти в `practice`
2. Вернуться в `education` относительным путём
3. Показать структуру проекта

## Ответ
```
vboxuser@mywm:~/linux_education$ tree practice/ -ahugD
[vboxuser vboxuser 4.0K Jul  4 07:35]  practice/
└── [vboxuser vboxuser 4.0K Jul  4 07:35]  education
    ├── [vboxuser vboxuser 4.0K Jul  4 07:35]  backup
    ├── [vboxuser vboxuser 4.0K Jul  4 07:35]  linux
    │   ├── [vboxuser vboxuser 4.0K Jul  4 07:35]  homework
    │   ├── [vboxuser vboxuser 4.0K Jul  4 07:59]  practice
    │   │   └── [vboxuser vboxuser    0 Jul  4 07:44]  task2.sh
    │   └── [vboxuser vboxuser 4.0K Jul  4 07:44]  theory
    │       └── [vboxuser vboxuser    0 Jul  4 07:44]  commands.txt
    └── [vboxuser vboxuser 4.0K Jul  4 08:02]  scripts
        ├── [vboxuser vboxuser    0 Jul  4 07:44]  deploy.sh
        └── [vboxuser vboxuser    0 Jul  4 07:44]  task1.sh
```
---

---

# Задание 4

## Требуется:

Создать вложенную структуру одной командой:

```text
company/
├── backend/
├── frontend/
├── devops/
│   ├── nginx/
│   ├── docker/
│   └── kubernetes/
└── monitoring/
```

---

## Вопросы

1. Для чего используется ключ `-p`?
2. Что произойдёт без `-p`?

## Ответы
1. Для создания вложеных каталогов
2. Ошибка
---

# Часть 3 — Работа с файлами

---

# Задание 5

## Требуется:

Создать файлы:

```text
education/linux/theory/commands.txt
education/linux/practice/task1.sh
education/linux/practice/task2.sh
education/scripts/deploy.sh
```

---

## Дополнительно

Проверить:

- размеры файлов;
- дату создания;
- права доступа.

## Ответ

```
vboxuser@mywm:~/linux_education$ tree practice/ -ahugD
[vboxuser vboxuser 4.0K Jul  4 07:35]  practice/
└── [vboxuser vboxuser 4.0K Jul  4 07:35]  education
    ├── [vboxuser vboxuser 4.0K Jul  4 07:35]  backup
    ├── [vboxuser vboxuser 4.0K Jul  4 07:35]  linux
    │   ├── [vboxuser vboxuser 4.0K Jul  4 07:35]  homework
    │   ├── [vboxuser vboxuser 4.0K Jul  4 07:59]  practice
    │   │   └── [vboxuser vboxuser    0 Jul  4 07:44]  task2.sh
    │   └── [vboxuser vboxuser 4.0K Jul  4 07:44]  theory
    │       └── [vboxuser vboxuser    0 Jul  4 07:44]  commands.txt
    └── [vboxuser vboxuser 4.0K Jul  4 08:02]  scripts
        ├── [vboxuser vboxuser    0 Jul  4 07:44]  deploy.sh
        └── [vboxuser vboxuser    0 Jul  4 07:44]  task1.sh
```

---

---

# Задание 6

## Требуется:

Скопировать:

- `commands.txt`
- `deploy.sh`

в каталог `backup`.

---

## После выполнения ответить:

1. Удалился ли оригинальный файл?
2. Чем `cp` отличается от `mv`?

### Ответ
1. Нет
2. "cp" - Копирование, "mv" - перемещение/переименование
---

---

# Задание 7

## Требуется:

Скопировать каталог:

```text
practice
```

внутрь:

```text
backup
```

---

## Вопросы

1. Почему используется `-r`?
2. Что означает recursive?


## Ответ

1. Для рекурсивного копирования
2. Вовлечение всех дочерних обьектов в процесс
---

# Часть 4 — Перемещение и переименование

---

# Задание 8

## Требуется:

1. Переместить:

    - `task1.sh`

    из:

```text
practice/
```

в:

```text
scripts/
```

2. Переименовать:

```text
deploy.sh → deploy_prod.sh
```

---

## После выполнения

Проверить:

- где теперь находятся файлы;
- изменились ли их inode.

## Ответ
inode не меняется

---

# Часть 5 — Удаление объектов

---

# Задание 9

## Требуется:

1. Удалить файл:

    - `task2.sh`

2. Удалить каталог:

    - `homework`

3. Удалить каталог:

    - `backup`

---

## Вопросы

1. Чем отличается:

    - `rmdir`
    - `rm -r`

2. Почему команда `rm -rf` считается опасной?


## Ответ
1. "rmdir" - Не удаляет не пустые каталоги. "rm -r" - Удаляет рекурсивно файлы и каталоги
2. Потому что указан флаг -f = force то есть принудительно
---

# Часть 6 — Ссылки

---

# Задание 10

## Требуется:

1. Создать файл:

```text
nginx.conf
```

2. Создать:

    - symbolic link
    - hard link

3. Проверить результат командой:

```bash
ls -li
```

---

## Ответить письменно

1. Чем отличается soft link от hard link?
2. Что произойдёт с symbolic link после удаления оригинального файла?
3. Почему hard link имеет тот же inode?

## Ответ
1. Хард линк ссылается на inode а софт линк создает как бы ярлык
2. Ничего. Просто станет не рабочей
3. Не понимаю вопрос. Хард линк это буквально еще одно имя для inode
---

# Часть 7 — Исследование системы

---

# Задание 11

## Требуется:

Исследовать содержимое каталогов:

```text
/usr/bin
/bin
/sbin
```

---

## Нужно:

1. Вывести содержимое:

    - `ls`
    - `ls -l`

2. Найти:

    - `cp`
    - `mv`
    - `rm`
    - `mkdir`

3. Определить тип файлов командой:

```bash
file
```

---

## Пример

```bash
file /bin/ls
```
## Ответ
LF 64-bit LSB pie executable
---

# Часть 8 — Практика DevOps

---

# Задание 12

## Ситуация

Вы — junior DevOps engineer.

Нужно подготовить структуру проекта:

```text
infra-project/
├── nginx/
├── docker/
├── kubernetes/
├── monitoring/
└── backup/
```

---

## Требуется

1. Создать структуру

2. Создать файлы:

    - `nginx.conf`
    - `Dockerfile`
    - `deployment.yaml`
    - `prometheus.yml`

3. Скопировать все конфиги в `backup`

4. Создать symbolic link на `nginx.conf`

5. Удалить каталог `docker`

6. Показать итоговую структуру проекта


## Ответ
```
vboxuser@mywm:~/linux_education/practice$ mkdir -p infra-project/{nginx,docker,kubernetes,monitoring,backup}
vboxuser@mywm:~/linux_education/practice$ ls
total 16K
drwxrwxr-x 4 vboxuser vboxuser 4.0K Jul  4 11:37 .
drwxrwxr-x 7 vboxuser vboxuser 4.0K Jun 30 13:37 ..
drwxrwxr-x 5 vboxuser vboxuser 4.0K Jul  4 07:35 education
drwxrwxr-x 7 vboxuser vboxuser 4.0K Jul  4 11:37 infra-project
vboxuser@mywm:~/linux_education/practice$ tree infra-project/
infra-project/
├── backup
├── docker
├── kubernetes
├── monitoring
└── nginx

6 directories, 0 files
vboxuser@mywm:~/linux_education/practice$ touch infra-project/{nginx/nginx.conf,docker/Dockerfile}
vboxuser@mywm:~/linux_education/practice$ tree infra-project/
infra-project/
├── backup
├── docker
│   └── Dockerfile
├── kubernetes
├── monitoring
└── nginx
    └── nginx.conf

6 directories, 2 files
vboxuser@mywm:~/linux_education/practice$ touch infra-project/{nginx/nginx.conf,docker/Dockerfile,monitoring/prometheus.yml,docker/deployments.yaml}
vboxuser@mywm:~/linux_education/practice$ tree infra-project/
infra-project/
├── backup
├── docker
│   ├── deployments.yaml
│   └── Dockerfile
├── kubernetes
├── monitoring
│   └── prometheus.yml
└── nginx
    └── nginx.conf

6 directories, 4 files
vboxuser@mywm:~/linux_education/practice$ cp infra-project/{nginx/nginx.conf,docker/Dockerfile,monitoring/prometheus.yml,docker/deployments.yaml} backup
cp: target 'backup': No such file or directory
vboxuser@mywm:~/linux_education/practice$ cp infra-project/{nginx/nginx.conf,docker/Dockerfile,monitoring/prometheus.yml,docker/deployments.yaml} infra-project/backup/
vboxuser@mywm:~/linux_education/practice$ tree infra-project/
infra-project/
├── backup
│   ├── deployments.yaml
│   ├── Dockerfile
│   ├── nginx.conf
│   └── prometheus.yml
├── docker
│   ├── deployments.yaml
│   └── Dockerfile
├── kubernetes
├── monitoring
│   └── prometheus.yml
└── nginx
    └── nginx.conf

6 directories, 8 files
```
---

# Теоретические вопросы

---

## 1. Что такое файловая система Linux?
1. Структура каталогов и файлов где филосовия "Все есть файл" 
(Каталог - файл в котором хранятся ссылки на inodes)
---

## 2. Что такое текущий рабочий каталог?
2. 
---

## 3. Чем абсолютный путь отличается от относительного?

---

## 4. Что делает команда `cd -`?
4. Возвращает пользователя в предыдущий каталог 
---

## 5. Для чего используется команда `ls -l`?
5. Для подробного вывода содержания каталога
---

## 6. Что такое inode?
6. Место в памяти отведенное под данные
---

## 7. Чем Linux отличается от Windows в работе с путями?

---

## 8. Почему не рекомендуется использовать пробелы в именах файлов?

---

# Дополнительное задание (по желанию)

## Исследование скрытых файлов

### Требуется:

1. Перейти в домашний каталог
2. Вывести скрытые файлы
3. Найти:

    - `.bashrc`
    - `.profile`
    - `.ssh`

---

## Вопросы

1. Почему файлы начинаются с точки?
2. Для чего используется `.bashrc`?
3. Что хранится в `.ssh`?

---

# Рекомендации

Во время выполнения домашней работы использовать:

- `man`
- `--help`
- историю команд (`↑`)
- автодополнение TAB

---

# Команды, которые должны быть освоены

| Команда | Назначение                   |
| ------- | ---------------------------- |
| `pwd`   | Текущий каталог              |
| `cd`    | Переход между каталогами     |
| `ls`    | Просмотр содержимого         |
| `mkdir` | Создание каталогов           |
| `touch` | Создание файлов              |
| `cp`    | Копирование                  |
| `mv`    | Перемещение/переименование   |
| `rm`    | Удаление                     |
| `rmdir` | Удаление пустых каталогов    |
| `ln`    | Создание hard link           |
| `ln -s` | Создание symbolic link       |
| `file`  | Определение типа файла       |
| `tree`  | Просмотр структуры каталогов |
