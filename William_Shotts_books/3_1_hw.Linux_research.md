# Домашнее задание №3

# Исследование системы Linux

---

# Цель домашней работы

Закрепить навыки:

- исследования файловой системы Linux;
- анализа файлов и каталогов;
- использования команд:

    - `ls`
    - `file`
    - `less`

- чтения системных файлов и логов;
- понимания структуры Linux.

---

# Правила выполнения

1. Все задания выполнять через терминал.
2. Не использовать графический файловый менеджер.
3. После каждой команды анализировать результат.
4. Ответы на вопросы оформить письменно.
5. Все команды вводить вручную.

---

# Часть 1 — Исследование домашнего каталога

---

# Задание 1

## Требуется

Перейти в домашний каталог и выполнить:

```bash id="dz3_1"
ls
```

```bash id="dz3_2"
ls -l
```

```bash id="dz3_3"
ls -a
```

---

## После выполнения

Определить:

1. Какие файлы скрытые;
2. Какие каталоги присутствуют;
3. Чем отличается вывод команд.

---

## Ответить письменно

1. Что показывает команда `ls`?
2. Что показывает `ls -l`?
3. Какие файлы отображает `ls -a`?
4. Почему скрытые файлы начинаются с точки?

1. Список файлов в каталоге
2. Список файлов с подробной информацией о правах, группах, дате и времени создания файла
3. Скрытые
4. Так устроена команда ls что есть проверка на то что все файлы начинающиеся с "." не показываются по умолчанию
так как "." обозначает текущй каталог
---

# Часть 2 — Подробный вывод файлов

---

# Задание 2

## Требуется

Выполнить:

```bash id="dz3_4"
ls -l /etc
```

---

## Изучить вывод

Найти:

- каталоги;
- обычные файлы;
- ссылки.

---

## Ответить письменно

1. Что означает первый символ:

    - `d`
    - `-`
    - `l`

2. Что означают права:

```text id="dz3_5"
rwxr-xr-x
```

3. Где отображается:

    - владелец;
    - размер файла;
    - дата изменения?


1. "d" - обозначает каталог, "-" - файл, "l" - симлинк
---

# Часть 3 — Исследование системных каталогов

---

# Задание 3

## Требуется

Исследовать каталоги:

```text id="dz3_6"
/bin
/usr/bin
/etc
/var/log
```

---

## Для каждого каталога

Выполнить:

```bash id="dz3_7"
ls
```

и:

```bash id="dz3_8"
ls -l
```

---

## После исследования ответить

1. Где находятся системные команды Linux?
2. Где хранятся конфигурационные файлы?
3. Где находятся системные логи?
4. Почему каталог `/var/log` важен для администратора?

1. /usr/bin
2. /etc
3. /var/log (syslog)
4. Важен для отладки ПО
---

# Часть 4 — Определение типов файлов

---

# Задание 4

## Требуется

Определить тип файлов:

```text id="dz3_9"
/bin/bash
/etc/passwd
/etc/hosts
/bin/ls
```

---

## Использовать

```bash id="dz3_10"
file
```

---

## Пример

```bash id="dz3_11"
file /bin/bash
```

---

## Ответить письменно

1. Что делает команда `file`?
2. Чем текстовый файл отличается от бинарного?
3. Что такое исполняемый файл?


1. Показывает подробную информацию о файле
2. Бинарный отмечается как ELF. Текстовый отмечается как text
3. Файл который исполняется компьютером
---

# Часть 5 — Работа с less

---

# Задание 5

## Требуется

Открыть файл:

```text id="dz3_12"
/etc/passwd
```

через:

```bash id="dz3_13"
less /etc/passwd
```

---

## Во время работы в less

Попробовать:

- прокрутку вниз;
- прокрутку вверх;
- поиск;
- выход.

---

## Использовать поиск

Найти:

```text id="dz3_14"
root
```

---

## Ответить письменно

1. Для чего используется `less`?
2. Почему `less` удобнее `cat` для больших файлов?
3. Как выйти из less?
4. Как выполняется поиск?


1. Для просмотра файла постранично
2. Можно пролистывать файл вперед и назад
3. q
4. "/" или "?"
---

# Часть 6 — Исследование логов

---

# Задание 6

## Требуется

Изучить каталог:

```text id="dz3_15"
/var/log
```

---

## После этого

Открыть один из логов:

```bash id="dz3_16"
less /var/log/syslog
```

или:

```bash id="dz3_17"
less /var/log/auth.log
```

---

## Исследовать

1. Какие записи присутствуют в логе;
2. Есть ли:

    - ошибки;
    - предупреждения;
    - информация о входе в систему.

---

## Ответить письменно

1. Для чего Linux использует логи?
2. Почему логи важны для DevOps и администраторов?
3. Что может храниться в `/var/log/auth.log`?

1. Не понимаю вопрос
2. Для отладки и отслеживания исполнения программ
3. Логи авторизации
---

# Часть 7 — Исследование исполняемых файлов

---

# Задание 7

## Требуется

Найти команды:

```text id="dz3_18"
ls
cp
mv
mkdir
```

в каталоге:

```text id="dz3_19"
/bin
```

---

## После этого

Определить тип каждой команды через:

```bash id="dz3_20"
file
```

---

## Пример

```bash id="dz3_21"
file /bin/ls
```

---

## Ответить письменно

1. Почему команды Linux являются обычными файлами?
2. Что значит:

```text id="dz3_22"
ELF executable
```

3. Чем shell отличается от исполняемого файла?

1. Потому что программа которую вызывает команда, представлена в виде бинароного файла
2. Исполняемый файл
3. Shell если я правильно понимаю это не конкретная программа а лишь тип/вид программы интерпритатора команд
---

# Часть 8 — Практика исследования файлов

---

# Задание 8

## Требуется

Создать структуру:

```text id="dz3_23"
research/
├── notes.txt
├── config.conf
├── script.sh
└── logs.log
```

---

## После создания

1. Вывести подробный список файлов;
2. Определить тип файлов;
3. Открыть один из файлов через `less`.

---

## Использовать команды

```bash id="dz3_24"
ls -l
```

```bash id="dz3_25"
file
```

```bash id="dz3_26"
less
```

---

# Часть 9 — Практика системного администратора

---

# Задание 9

## Ситуация

Вы — junior Linux administrator.

Необходимо:

1. Исследовать систему;
2. Найти:

    - системные команды;
    - конфиги;
    - логи;
    - исполняемые файлы.

---

## Для этого исследовать:

```text id="dz3_27"
/bin
/etc
/var/log
/usr/bin
```

---

## После исследования ответить

1. Где Linux хранит настройки системы?
2. Где находятся программы?
3. Где искать ошибки системы?
4. Почему умение читать логи важно для DevOps?

---

# Теоретические вопросы

---

## 1. Для чего используется команда `ls`?

---

## 2. Что показывает `ls -l`?

---

## 3. Какие файлы показывает `ls -a`?

---

## 4. Что делает команда `file`?

---

## 5. Для чего используется `less`?

---

## 6. Чем `less` лучше `cat` для больших файлов?

---

## 7. Что означает:

- `d`
- `-`
- `l`

в выводе `ls -l`?

---

## 8. Где Linux хранит системные логи?

---

## 9. Что такое исполняемый файл?

---

## 10. Почему Linux рассматривает команды как файлы?

---

# Дополнительное задание (по желанию)

## Исследование man-страниц

Изучить справку для команд:

```text id="dz3_28"
ls
file
less
```

---

## Использовать

```bash id="dz3_29"
man ls
```

---

## Требуется

Для каждой команды написать:

1. Назначение;
2. Полезные ключи;
3. Пример использования.

---

# Команды, которые должны быть освоены

| Команда | Назначение          |
| ------- | ------------------- |
| `ls`    | Просмотр файлов     |
| `ls -l` | Подробный вывод     |
| `ls -a` | Скрытые файлы       |
| `file`  | Тип файла           |
| `less`  | Просмотр файла      |
| `q`     | Выход из less       |
| `/`     | Поиск в less        |
| `man`   | Справка по командам |
